pipeline {
    agent any





   stages {
        stage('Test') {
            steps {
                // Run tests, if applicable
                sh 'python -m pytest tests'
            }
        }
    }
}
